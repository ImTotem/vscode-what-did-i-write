# 전체 목표

거대한 오픈소스를 포크해 개발할 때, 복잡한 Git 로그를 뒤지지 않고 **현재 코드 속에서 내가 추가하거나 수정한 부분과 그 과거 기록만 바로 확인할 수 있는 VS Code 확장 프로그램**을 만든다.

핵심 개념은 다음과 같다.

> Git 커밋 중심이 아니라 **현재 코드 중심**으로 내 변경사항을 보여준다.

## 사용자 경험

확장을 설치하고 Git 저장소를 열면 별도 설정 없이 자동으로 동작한다.

```text
Git 신원 자동 확인
        ↓
내 커밋과 변경 파일 검색
        ↓
Explorer에 A/M 표시
        ↓
파일을 열면 내 코드 라인 표시
        ↓
필요할 때 해당 파일·라인의 과거 기록 확인
```

기준 커밋 선택, 브랜치 설정, 계정 연결 같은 초기 설정은 없다.

## 내 변경을 판단하는 기준

다음 전역 Git 설정을 현재 사용자 신원으로 사용한다.

```bash
git config --global user.name
git config --global user.email
```

현재 체크아웃된 `HEAD`에서 도달할 수 있는 커밋 가운데 작성자 이름 또는 이메일이 일치하는 커밋만 내 기록으로 판정한다.

- 다른 사람이 작성한 커밋은 제외
- 다른 브랜치의 관계없는 커밋은 제외
- staged·unstaged·untracked 변경은 현재 사용자의 작업으로 취급
- 기준점이나 커밋 날짜는 사용하지 않음

## Explorer 표시

기존 VS Code Explorer에서 내 코드가 포함된 파일과 폴더를 바로 구분한다.

```text
EXPLORER
▾ src                       ●
    auth.ts                  M
    oauth.ts                 A
    upstream.ts

▾ tests                     ●
    oauth.test.ts            A

▾ MY CODE
    CURRENT
      auth.ts                M
      oauth.ts               A
      oauth.test.ts          A

    PAST ACTIVITY
      old-config.ts          ◷
```

표시 의미:

- `A`: 내 커밋에서 생성했고 현재도 존재하는 파일
- `M`: 현재 파일에 내가 작성한 라인이 남아 있음
- `◷`: 과거에 내가 수정했지만 현재는 원복되거나 다른 코드로 교체됨
- 폴더 장식: 내부에 내 코드가 포함된 파일이 존재
- 변경되지 않은 오픈소스 파일은 평소처럼 표시

`MY CODE` 영역에서는 내 파일만 따로 모아볼 수 있다.

## 코드 편집기

파일을 열면 별도의 잘린 diff가 아니라 **현재 완성된 코드 전체**를 보여준다.

```text
20 │ 원본 또는 다른 사람의 코드
21┃ 내가 작성한 코드
22┃ 내가 작성한 코드
23 │ 다른 사람이 나중에 수정한 코드
```

내 코드 라인은 다음 위치에 표시한다.

- 편집기 gutter
- overview ruler 또는 minimap
- 선택 사항으로 라인 배경 강조

표시 여부는 현재 파일의 `git blame` 결과와 내 Git 신원을 비교해 판단한다.

라인에 마우스를 올리거나 선택하면 다음 정보를 보여준다.

- 작성 커밋
- 작성 날짜
- 커밋 메시지
- 해당 라인의 이전 변경 기록

## 과거 기록

전체 Git 로그를 시간순으로 보여주지 않는다. 현재 선택한 파일이나 코드와 관련된 **내 기록만 문맥에 맞게** 표시한다.

```text
HISTORY: auth.ts

● 2026-08-20  토큰 갱신 오류 수정
● 2026-08-12  OAuth API 변경
● 2026-08-03  예외 처리 추가
● 2026-07-28  파일 최초 생성
```

- 다른 사람의 커밋은 기본적으로 제외
- 파일을 변경한 내 커밋만 표시
- 선택한 기록의 상세 diff는 필요할 때만 열기
- 현재 남아 있지 않은 과거 변경도 `Past Activity`에서 확인 가능

## 성능 방식

거대 저장소에서도 사용할 수 있도록 전체 파일에 무조건 `git blame`을 실행하지 않는다.

1. 내 커밋이 건드린 파일 목록을 먼저 수집
2. 해당 파일만 현재 상태 검사
3. 열린 파일과 Explorer에 보이는 항목을 우선 처리
4. 결과를 저장해 재사용
5. 파일 저장, 커밋, checkout, rebase 등에 맞춰 필요한 부분만 갱신

모든 분석은 로컬 Git으로 수행한다. GitHub 계정이나 외부 서버는 필요 없다.

## MVP에서 제외할 기능

첫 버전에서는 다음을 만들지 않는다.

- 기준 커밋 선택
- 커밋 그래프
- VS Code 또는 Code - OSS 포크
- GitLens 같은 외부 확장 의존성
- AI와 사람의 작업 구분
- 여러 파일을 기능 단위로 자동 묶는 Change Story
- 클라우드 계정 및 원격 데이터 저장
- 모든 브랜치 통합 분석

## 성공 기준

- 확장 설치 후 별도 설정 없이 동작한다.
- 거대한 오픈소스에서 내 코드가 포함된 파일만 즉시 찾을 수 있다.
- 파일을 열면 전체 코드 문맥을 유지하면서 내 라인이 보인다.
- 이미 커밋한 변경과 작업 중인 변경을 함께 표시한다.
- 현재 코드와 관련된 내 과거 기록만 확인할 수 있다.
- 일반적인 개발 과정에서 Explorer 표시가 자동으로 갱신된다.

한 문장으로 줄이면:

> **현재 체크아웃된 오픈소스 코드에서 내 Git 신원으로 작성된 파일·라인·과거 기록만 자동으로 찾아, VS Code Explorer와 편집기 안에 자연스럽게 표시하는 무설정 확장 프로그램이다.**
